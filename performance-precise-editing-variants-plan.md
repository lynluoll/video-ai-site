# Performance Precise Editing：四个视频变体计划

> 母版：Seedance 2.5 TikTok UGC Shoppable Video，9:16，24s
> Task ID：`cgt-20260814175223-vs2bw`

## 核心展示逻辑

四条变体都从**同一支母版独立编辑**，不串联修改。每条只替换一个变量，其他内容全部锁定：

- 镜头与构图不变
- 人物动作与表演节奏不变
- Hook → Product Proof → CTA 的时间点不变
- 未指定的商品、文字、声音、人物和环境不变

页面最终用 `MASTER + 4 VARIANTS` 并列展示，让用户直接理解“保留胜出结构，只改一个变量”。

## Variant 01 — SKU Replacement

**只替换商品。**

- 将母版中的白色便携榨汁机替换为一款新的虚拟 SKU，第一版使用**钴蓝色便携榨汁机**。
- 新 SKU 先制作一张干净的商品参考图，明确杯体、刀座、杯盖、按键和材质。
- 保留人物、手势、握持关系、榨汁动作、镜头、背景、对白、音乐、字幕与 CTA 不变。
- 所有出现商品的镜头都必须完成一致替换，不能只改某一个特写。

**要证明：** 商品可以被精确替换，同时手部交互和整条广告结构保持稳定。

## Variant 02 — Text Replacement

**只替换画面文字。**

- 先逐帧记录母版中 Hook、卖点和 CTA 的原始文字及出现时间。
- 将现有文案替换为一套更直接的效果广告文案，例如：
  - Hook：`BLEND ANYWHERE`
  - CTA：`SHOP NOW`
- 保留文字原有位置、字号层级、出现时点和动效节奏。
- 人物、商品、对白、音乐、动作、背景和镜头全部不变。

**要证明：** 胜出视频可以快速更换广告信息和 CTA，不需要重新生成整支视频。

## Variant 03 — Dialogue Replacement

**只替换人物的一句话。**

- 将原句 `Press once and it blends right in the cup.` 替换为 `Press once and your smoothie is ready in seconds.`。
- 使用原人物的原始音色，只改这一句话及对应口型，不引入新的 voice reference。
- 其他对白逐字保留；音乐、环境声和产品音效保持不变。
- 人物身份、表情节奏、肢体动作、商品、画面文字、镜头和剪辑保持母版原样。

**要证明：** 同一条胜出素材可以精确修改一句销售话术，而不重做整条视频。

## Variant 04 — Person Replacement

**只替换出镜人物。**

- 将母版人物替换为一名新的虚拟 UGC creator，第一版采用**东南亚年轻女性创作者**。
- 人物参考使用同账号近期生成的可信虚拟人物素材，不使用真实人物原始照片。
- 参考图只定义新人物身份与面部外观；服装继续使用母版的奶油色卫衣。
- 保留原人物的站位、视线、动作、手势、口型时点和商品交互。
- 商品、对白、声音、文字、背景、镜头和剪辑节奏全部不变。

**要证明：** 可以面向不同受众替换 creator，同时保持商品和广告结构一致。

## 制作流程

1. 下载并保存母版 MOV/MP4 到 `public/media/performance-precise-editing/master/`。
2. 对母版做一次逐镜头审计：对白、画面文字、CTA、商品出现区间、人物正脸区间。
3. 准备 SKU 与虚拟人物参考素材，并先做静态一致性校验。
4. 四条任务均使用 Seedance 2.5 `task_type="edit"`：`ratio=adaptive`、`duration=-1`、优先输出 MOV；Dialogue 与 Human 失败项各并发重试三次，由人工选择最佳结果。
5. 四条任务分别从母版提交，每条 prompt 明确 `A → B`、编辑范围与 `keep everything else unchanged`。
6. 下载成片，抽帧并与母版做并排 QA；通过后再压制网站播放用 MP4。

## 验收标准

- 每条片只发生目标变量变化，不出现额外重绘。
- 商品外观跨镜头一致，手指不穿模，握持与使用动作合理。
- 人物脸部、身体比例和动作连续；人物替换版身份稳定。
- 文字拼写正确、清晰，无乱码，出现时间与母版一致。
- 音频自然、无断裂；对白口型同步，音乐和环境声没有意外变化。
- 四条片的时长、镜头数量、剪辑点和 CTA 时点与母版基本一致。

## 风险与回退

- Seedance 2.5 编辑对不超过 20 秒的输入更稳定，而当前母版为 24 秒。
- 第一轮仍按完整 24 秒母版生成；如果出现后半段漂移或变量污染，则剪出一条保留完整 `Hook → Proof → CTA` 的 16–20 秒标准版，再从该版本并行重做四条编辑。
- 不把多个修改合并进同一条任务，也不使用上一条变体作为下一条的输入。
